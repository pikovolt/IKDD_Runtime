knowledge:
  CSV_LOAD:
    description: "CSV を dict形式で読み込む"
    hint: "csv.DictReader を使う"

  FILTER_ROWS:
    description: "行のフィルタリング処理"
    hint: "list comprehension を使うとよい"

  JSON_EXPORT:
    description: "JSON に保存する"
    hint: "json.dump で書き出す"
