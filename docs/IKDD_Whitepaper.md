# Intent-First Development / IKDD Whitepaper (Public Draft)

**Intent is the Asset.
Implementation is Disposable.**

Author: **Shouichi Kanbara (pikovolt)**
Date: **2025-11-08**
License: **Public concept – shareable with attribution**

---

## ❓ なぜこの文書を書いたのか

現在の AI コード生成は **HOW（実装）** の自動化に集中している。

しかし、開発で最も時間を奪っているのは **実装を書くことではない**。

> ✅ 何をしたいか（Intent）を形式化する作業
> ✅ 仕様と実装の乖離
> ✅ 実装を守り続ける保守コスト

**Intentこそが本当の開発資産**である。

---

## 🚀 概念（Core Idea）

> **Intent は "書くもの" ではない。話すだけでいい。
Intent 定義（構造）は AI が生成し、実装は副産物として再生成できる。**

---

## 1. Background — 従来の AI は「実装自動化」で止まっている

従来のフロー：

```
人間が Intent/仕様を書く
↓
AI が HOW（実装）を書く
```

問題：

| 問題 | 説明 |
|------|------|
| Intent が暗黙知に閉じる | "何をしたいのか" を形式化できない |
| README / 実装 / 手順がバラバラ | ドキュメント負債 |
| コードが「資産＝壊せない」 | 保守コストが永続する |

---

## 2. Insight — **Intent は「書く」のではなく、AI が「生成する」**

Intent-First Development はこう変える：

```
人：やりたいこと（WHAT）を話す
↓
AI：Intent を生成（構造化）
↓
AI：実装を生成（HOW）
```

> 人は **WHAT** に集中。
> AI が **HOW** を考える。

---

## 3. Intent OS — WHAT を扱う OS

Intent OS がやること：

- 人間の曖昧な意図をヒアリング
- **Intent 定義 (goal / target / Done)** を生成
- 実装コードはこの Intent から生成される

例：

```yaml
Intent:
  goal: insert null as parent
  target: SelectedModel
```

> ✅ Intent は **人間が書かない**
> ✅ Intent は **AI が生成する**

---

## 4. IKDD — HOW（知識）を道具化する開発方式

**IKDD = Instrumental Knowledge Driven Development**

* HOW（操作 / API / スクリプト）を **道具（Instrument）** として扱う
* HOW は Intent によって **選択 / 組み立て / 捨てられる**
* 実装は **消耗品（replaceable）**

| Before（旧開発） | After（IKDD）  |
| ----------- | ------------ |
| 実装が本番資産     | Intent が本番資産 |
| 実装を守る       | 実装を捨てられる     |
| HOW を考える    | WHAT を伝えるだけ  |

**知識を道具化すると、実装は再生成できるものになる。**

---

## 5. Intent OS × IKDD の関係（核心）

```
┌──────────┐
│ Intent OS │   ← WHAT を生成（AI）
└──────────┘
        ↓ Intent を渡す
┌──────────┐
│   IKDD    │   ← HOW を道具化 / 交換可能
└──────────┘
        ↓ 実装を自動生成（副産物）
Implementation (捨てられる)
```

* Intent OS → WHAT（意図）
* IKDD → HOW（知識の道具化）

> **Intent は永続、実装は揮発。**

---

## 6. Minimal Intent Schema（IKDDで使用）

```yaml
Intent:
  goal: <目的>
  target: <対象>

Done:
  - <Before-State> == <After-State>
```

人間はこれすら **書かない**。
Intent OS が自動生成する。

---

## 7. 結論

> **開発は「実装を書くこと」から「意図を伝えること」へ変わる**

* Intent = 資産
* 実装 = 捨てられる副産物
* HOW = AI が選び実装する

---

## Attribution

```
Concept Author: Shouichi Kanbara (pikovolt)
License: Public concept – shareable with attribution
```

---

```
✨ If you publish or reuse, please credit the author.
```

```
Intent-First Development / IKDD
```

---
