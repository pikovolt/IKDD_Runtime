# IKDD / CDD — Concept Document
> *実装を生成するのではなく、文脈(Context)を制御するための方法論*

---

## 1. 背景 — 「AI が実装を壊す問題」

AI がコードを生成するようになったが、現実には以下の問題が残っている。

| 問題 | 説明 |
|------|------|
| 意味的ドリフト(Semantic Drift) | 修正依頼や追記をするほど、意図がずれる |
| 実装に引っ張られる | HOW を書き始めると WHY / WHAT が崩れる |
| プロンプト肥大 | 「説明」→「例」→「禁止条件」…どんどん肥大化し維持不能に |

例えると:

> AI に実装を「任せる」のではなく、
> AI に「判断を丸投げしてしまっている」。

---

## 2. IKDD:Instrumental Knowledge Driven Development
> **"実装ではなく、道具を扱う。"**

IKDD の核となる考え方:

> *HOW(実装)を一枚のドキュメントに書かない。
> WHY / WHAT と切り離し、再利用可能な **道具(knowledge)** として扱う。*

---

### IKDD の構造

| ファイル | 役割 |
|----------|------|
| `tool.yaml` | WHY / WHAT(意図・目的・期待する状態) |
| `knowledge.yaml` | HOW(実装の部品) |
| `generated/*.py` | IKDD Runtime が生成する決定論コード |

---

### IKDD が解決すること

| 従来の LLM コード生成 | IKDD |
|------------------------|------|
| 「指示に従ってコードを書いて」 | 「意図と道具を渡すので、構成して」 |
| AI が推論する | IKDD Runtime が決定論的に構成 |
| 意図がドリフトする | 意図はファイル(tool.yaml)で固定される |
| 例を示して説明する | knowledge として declarative に定義 |

---

## 3. CDD:Context Driven Design / Context Constraint Design

IKDD を基礎として生まれた考え方:

> **「実装を設計するのではなく、文脈(Context)を設計する」**

### Context = 実装を生成する枠

- 「Context を設計する」とは
  - AI が自由に推論しないように
  - **曖昧性の無い範囲(枠)** を設計すること

---

### CDD が導入する概念:**Context Constraint**

例:

```yaml
constraints:
  must_use: [CSV_LOAD, FILTER_ROWS, JSON_EXPORT]
  forbidden_imports: [pandas]
  immutable_params: [filter_column]
```

CDD を導入すると:

* 「この実装はこの道具を必ず使う」
* 「この外部依存は禁止」
* 「この変数は変えてはいけない」

という、**意図を守るための制約** が定義できる。

---

## 4. IKDD Runtime が提供するもの

| 要素                      | 説明                                   |
| ----------------------- | ------------------------------------ |
| `tool.yaml` → 実装構成を宣言   | WHAT/WHY のみを書く。HOW は書かない             |
| `knowledge.yaml` → 道具   | 再利用可能な部品(snippet/few-shot)           |
| `IKDD Runtime` → 決定論的生成 | AIを使わず deterministic code generation |

フロー定義(例):

```yaml
flow:
  - step: CSV_LOAD
    input: [csv_file]
    output: rows

  - step: FILTER_ROWS
    input: [rows, threshold]
    output: filtered

  - step: JSON_EXPORT
    input: [filtered, json_file]
    output:
```

---

## 5. IKDD / CDD の本質

> **意図(WHY/WHAT)を固定し、実装(HOW)を差し替え可能にする。**

これは、OOP のような**抽象化**とも似ているが、決定的に違う点がある。

| OOP            | IKDD            |
| -------------- | --------------- |
| 抽象化をプログラミングで表現 | 抽象化を YAML で宣言   |
| 実装時に抽象を守る      | 実装は Runtime が構成 |
| 「実装者」が抽象を守る    | 「枠」が抽象を守る       |

---

## 6. IKDD のコア価値

* 実装は knowledge として再利用可能
* 意図(WHY/WHAT)は tool.yaml で永続化
* CDD により **変更に強い**

> **実装ではなく、文脈と道具を設計する。**

---

## 7. Roadmap

| Version | 内容                                             |
| ------- | ---------------------------------------------- |
| ✅ v0.1  | deterministic code generator(flow + knowledge) |
| 🔜 v0.2 | Context Constraint DSL(must/forbidden/keep)    |
| 🔮 v0.3 | optional type validation                       |
| 🔭 v0.4 | knowledge versioning / reuse / registry        |

---

## 8. IKDD / CDD をひとことで表すと

> **AI を制御するテンプレートではない。
> 意図と文脈を固定する "設計手法"。**

```
設計者 → Context(枠)を設計
Runtime → 決定論的に実装生成
AI →(任意で)knowledge の生成に使える
```

---

### IKDD がもたらす変化

| Before       | After (IKDD/CDD)            |
| ------------ | --------------------------- |
| AI に実装をお願いする | AI を **道具の管理下に置く**          |
| 実装担当         | **Context 設計者**             |
| 推論-based     | **構成-based(deterministic)** |

---

> **IKDD は「AI に任せる」時代から、「AI を扱う」時代をつくる。**

```
Implementation → Knowledge
Prompt → Context
Code → Composition
```

---

```
Author: pikovolt
IKDD Runtime / IKDD / CDD Concept Document
License: MIT
```
